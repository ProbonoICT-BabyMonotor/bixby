endpoints {
  action-endpoints {
    action-endpoint (Login){
      accepted-inputs ()
      local-endpoint (Login.js)
    }
    
    action-endpoint (backdraft){
      accepted-inputs ()
      local-endpoint (backdraft.js)
    }
  }
}